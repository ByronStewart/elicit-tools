import { ItemSubClassPipe } from './item-sub-class.pipe';

describe('ItemSubClassPipe', () => {
  it('create an instance', () => {
    const pipe = new ItemSubClassPipe();
    expect(pipe).toBeTruthy();
  });
});
