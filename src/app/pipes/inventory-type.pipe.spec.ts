import { InventoryTypePipe } from './inventory-type.pipe';

describe('InventoryTypePipe', () => {
  it('create an instance', () => {
    const pipe = new InventoryTypePipe();
    expect(pipe).toBeTruthy();
  });
});
